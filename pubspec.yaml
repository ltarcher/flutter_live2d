name: flutter_live2d
description: "A new Flutter plugin project."
version: 0.0.1

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.plugin.flutter_live2d
        pluginClass: FlutterLive2dPlugin
      ios:
        pluginClass: FlutterLive2dPlugin
      web:
        pluginClass: FlutterLive2dWeb
        fileName: flutter_live2d_web.dart
